name: "Manara Backend"
version: "0.3.13"
slug: "manara_backend"
description: "Manara smart building automation backend"
arch: [amd64]
startup: application
boot: auto
init: false
ports:
  8000/tcp: 8000
ingress: true
ingress_port: 8000
homeassistant_api: true
options:
  log_level: "info"
  secret_key: ""
schema:
  log_level: "list(debug|info|warning|error)"
  secret_key: "str"
